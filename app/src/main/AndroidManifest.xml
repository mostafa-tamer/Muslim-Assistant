<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.muslimsAssistant">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:name="com.example.muslimsAssistant.MuslimsAssistantApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/pray"
        android:label="@string/app_name"
        android:roundIcon="@drawable/pray"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuranReminder"
        tools:targetApi="31">
        <activity
            android:name="com.example.muslimsAssistant.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".widgets.PrayerTimesWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider"
                android:resource="@xml/widget_provider" />
        </receiver>

        <receiver android:name="com.example.muslimsAssistant.receivers.PrayerTimesReceiver" />
        <receiver android:name="com.example.muslimsAssistant.receivers.WidgetReceiver" />
        <receiver android:name="com.example.muslimsAssistant.receivers.ReminderReceiver" />

        <service android:name=".widgets.PrayerTimesWidgetService"/>

    </application>

</manifest>